GROQ_API_KEY=YOUR_API_KEY

# Set to 1 only in production (after npm run build) to serve the frontend from FastAPI.
# Leave unset in development — Vite handles the frontend on port 5173.
# SERVE_FRONTEND=1
